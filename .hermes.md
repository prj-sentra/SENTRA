# S.E.N.T.R.A. 프로젝트 규칙

## 정체성

S.E.N.T.R.A.는 대화 기반 트레이딩 매매일지 및 복기 시스템이다.

사용자는 폼을 직접 작성하기보다 assistant와 대화하면서 매매를 기록한다. assistant는 근거 있는 매매 대화를 구조화된 API 호출로 변환한다.

배포 주소:

```text
https://sentra.hoya.kim
```

## 개발 방식

bottom-up 방식으로 개발한다.

가장 작은 유용한 단위를 만들고, 테스트하고, 필요하면 배포한 뒤 커밋한다.

기본 루프:

```text
실패하는 테스트 → 최소 구현 → build/typecheck/test → smoke test → commit
```

`npm`이 아니라 `pnpm`을 사용한다.

검증:

```bash
pnpm build && pnpm typecheck && pnpm test
```

배포 확인:

```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml config
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
```

검증된 작업 단위마다 커밋한다.

## 제품 범위

두 개의 도메인이 있다.

1. 매매일지
2. 트레이딩 지식 위키

매매일지를 먼저 만든다. 위키는 매매일지 핵심 기능이 안정될 때까지 스켈레톤 상태로 둔다.

## 매매 모델

사용자의 CFD 스타일 포지션 모델을 따른다.

```text
Trade = 독립 포지션 1개
Entry = 해당 포지션의 진입
Exit = 해당 포지션의 청산
```

진입과 청산은 반드시 분리한다.

같은 종목, 같은 방향의 포지션이 여러 개 공존할 수 있다.

중요 규칙:

```text
포지션 추가 = 새로운 독립 Trade 생성
```

포지션 추가를 아래 개념으로 모델링하지 않는다.

```text
추가매수
scale-in
평균 진입가
기존 trade의 수량 수정
```

“포지션 추가”는 사용자가 명확히 다르게 말하지 않는 한, 새로운 독립 포지션을 여는 것을 의미한다.

## Assistant 기반 API

사용자 대화에서 assistant가 조작하기 쉬운 API를 우선한다.

목표 흐름:

```text
사용자의 매매 발화
→ assistant가 근거 있는 필드 추출
→ assistant가 API action 전송
→ 원문 맥락 보존
→ 구조화된 trade 저장
```

근시일 내 endpoint:

```text
POST /trade-log/trades
POST /trade-log/trades/:id/entry
POST /trade-log/trades/:id/exit
POST /trade-log/assistant-actions
GET  /trade-log/trades
GET  /trade-log/trades/:id
```

누락된 매매 정보를 지어내지 않는다. 필요하면 질문하거나 선택 필드는 생략한다.

사용자 원문 맥락은 `note`, `thesis`, 또는 추후 raw-source 필드에 보존한다.

## 테스트 우선순위

실제 사용 방식부터 테스트한다.

1. conversation/action contract
2. 도메인 invariant
3. API controller
4. persistence
5. public smoke test

필수 invariant:

```text
진입 전 청산은 거부된다
같은 trade에 두 번째 진입은 거부된다
closed trade의 두 번째 청산은 거부된다
같은 종목/같은 방향 포지션 추가는 별도 trade를 만든다
필수 필드 누락은 명확히 실패한다
price/quantity는 제공된 경우 양수여야 한다
```

smoke test에는 합성 데이터를 사용한다. 저장소가 in-memory라면 테스트 데이터를 이후 제거한다.

## 영속성

in-memory 저장은 초기 API 형태 검증용으로만 허용한다.

Postgres 영속화가 구현되고 검증되기 전까지 durable journal이라고 말하지 않는다.

선호 영속화 방식:

```text
PostgreSQL + Prisma
```

최소 모델:

```text
Trade
TradeEntry
TradeExit
```

strategy/session/mistake/exchange 모델은 너무 이르게 추가하지 않는다.

## 안전

secret을 노출하지 않는다.

실거래 주문을 넣지 않는다. S.E.N.T.R.A.는 기록, 복기, 분석, 검증만 한다.

거래소 연동은 read-only credential을 사용한다.

## 보고 방식

완료 보고에는 아래를 포함한다.

- 변경된 파일/기능
- commit hash
- 실행한 테스트
- 배포/smoke-test 결과
- 남은 한계

실제로 만들고 검증하지 않은 것은 완료됐다고 말하지 않는다.
